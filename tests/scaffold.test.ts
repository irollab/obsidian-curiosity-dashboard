import { describe, expect, it } from 'vitest';

describe('plugin scaffold', () => {
  it('uses the curiosity-dashboard plugin id', () => {
    expect('curiosity-dashboard').toBe('curiosity-dashboard');
  });
});
